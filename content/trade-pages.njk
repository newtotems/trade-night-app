---
pagination:
  data: eventlist
  size: 1
  alias: eventtrade
permalink: "/event/trade/{{ eventtrade.id }}/"
layout: layouts/base-trade.njk
---

<div class="w-screen h-screen flex flex-col">
  <div class="flex-grow flex flex-col md:flex-row">
    <div id="trader" class="w-full h-1/2 md:w-1/2 md:h-full bg-gray-100 flex flex-col items-center justify-between py-4 relative">
      <div class="w-48 h-64 border-2 border-white border-dashed flex items-center justify-center">
      
      <div class="absolute bottom-0 left-0 right-0 flex justify-around overflow-hidden">
        {% for i in range(0, 6) %}
          <div class="w-1/6 p-2">
            <div class="rounded-lg aspect-[2/3] transform translate-y-1/2 max-h-[200px] overflow-hidden">
            <img src="/img/pokemon.jpg" alt="{{ eventtrade.name }}" class="w-full h-full object-cover">
            </div>
          </div>
        {% endfor %}
      </div>
      
      </div>
    </div>

    <div id="tradepartner" class="w-full h-1/2 md:w-1/2 md:h-full bg-gray-200 flex items-center justify-center relative">
     
 <div class="absolute bottom-0 left-0 right-0 flex justify-around overflow-hidden">
        {% for i in range(0, 6) %}
          <div class="w-1/6 p-2">
            <div class="rounded-lg aspect-[2/3] transform translate-y-1/2 max-h-[200px] overflow-hidden">
            <img src="/img/pokemon.jpg" alt="{{ eventtrade.name }}" class="w-full h-full object-cover">
            </div>
          </div>
        {% endfor %}
      </div>

    </div>
  </div>
  
  <!-- Updated tradedetail div with fixed height -->
  <div id="tradedetail" class="w-full h-[85px] md:h-[120px] bg-gray-700 flex">
    <div class="w-[35%] text-white">
        <p>trading</p>
        <p>£134.56</p>
    </div>
    <div class="w-[30%] flex items-center justify-center">
      <button class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded-lg text-lg md:text-xl lg:text-2xl shadow-lg w-full">
        Trade
      </button>
    </div>
    <div class="w-[35%] text-white">
        <p>recieving</p>
        <p>£1456.33</p>
    </div>
  </div>
</div>

